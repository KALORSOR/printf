#include "main.h"

int _printf(const char *format, ...);
{
int _deci = 0;
int _int = 0;

va_list args_list;

if(if format == NULL){
return (-1)};

va_start(args_list, format);

while (*format){
if(*format != '%'){
write(1,format,...)
}
else{
format++;
}
if(format=='d'){


}

}

}
